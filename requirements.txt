# conda create --prefix=./venv python=3.6
# Python 3.6.13 :: Anaconda, Inc.

# For linux follow below steps:
# sudo apt install python3-dev
# sudo apt install libmysqlclient-dev
# sudo apt install libssl-dev
# sudo apt install build-essential

# To generate a self-signed SSL certificate execute below command in Git Bash
# openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes

# Execute below commnads in MySQL
# CREATE DATABASE `user-system`;
# USE user-system;
# CREATE TABLE user (
#     userid INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
#     name VARCHAR(100) NOT NULL,
#     email VARCHAR(100) NOT NULL UNIQUE,
#     password VARCHAR(255) NOT NULL,
#     age_of_driver VARCHAR(100) NOT NULL,
#     vehicle_type VARCHAR(100) NOT NULL,
#     age_of_vehicle VARCHAR(100) NOT NULL,
#     engine_capacity_in_cc VARCHAR(100) NOT NULL,
#     gender VARCHAR(100) NOT NULL
# );

Flask==2.0.3
mysqlclient==1.3.14
Flask-MySQLdb==1.0.1
pandas==1.1.5
scikit-learn==0.22.1
vaex==4.17.0
toolz==0.12.0
seaborn==0.11.2
gmaps==0.9.0
bcrypt==4.0.1
python-dotenv==0.20.0